# Compatibility fixture

Preserve the Python v1 repository contract across the Rust cutover.

## Start here

Read `context.md` and `plan.md` before changing the project. Use the `zdev`
harness skill to continue the area; use `zd check delivery` to validate its local
state.
