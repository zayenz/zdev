---
description: Run one approved bounded parallel zdev task batch
argument-hint: <area> <task-id> <task-id> [...]
---

Load the Parallel route from the installed zdev skill and follow its shared
admission, isolation, serial integration, completion, stopping, and recovery
contract. Use `$ARGUMENTS` only as the requested area and finite task list; all
required user choices must already be explicit before source mutation.

The current Oh My Pi session is the coordinator. Before creating worktrees,
inspect the live `task` and `hub` tool descriptions. Continue in parallel only
when named agents are discoverable, workers can read explicit absolute source
and authoritative record paths, task calls are asynchronous, and `hub` exposes
job listing, waiting, and cancellation. Otherwise explain the missing control
and offer ordinary sequential Implement without creating a source tree.

Use the live Task schema. When batch mode exposes `{context,tasks:[...]}`, send
one item per worker with `name`, `agent`, `task`, `outputSchema`, and
`schemaMode`. When it exposes the flat item shape, spawn each worker separately
with those fields. Never send `isolated`: coordination creates and owns the
zdev source worktrees and supplies their absolute paths in each assignment.

Use blocking `zdev-planner` only for each required advanced plan. Dispatch
initial implementations through non-blocking `zdev-parallel-routine-implementer`,
`zdev-parallel-implementer`, or `zdev-parallel-advanced-implementer`, preserving
the configured profile and implementer result contract. Keep the existing
blocking `zdev-verifier` and ordinary one-task agents unchanged. Count active
background implementers and blocking planners, verifiers, and rework workers
together against the agreed total role limit.

Record every returned job and agent ID against its zdev task ID. Accept a
settled result only when its native identity, assignment, agent role, and
validated implementer object all identify that task. Consume arrivals one at a
time and run destination integration, fresh independent verification, rework,
completion, and commit serially before accepting another candidate.

Use `hub` job control for waits and cancellation. On cancellation, stop new
dispatch, request cancellation for every active job, then inspect jobs again to
confirm settlement. Preserve every unconfirmed or unfinished worktree. Revive
only a known same-session, non-isolated idle worker for ordinary rework; after
interruption or resume, use fresh roles and recover from retained Git records.

<!-- zdev:generated-repository-guidance:start -->
## Repository guidance discovery

Before inspecting, planning, changing, or validating code, read applicable repository and directory-specific `AGENTS.md` files, `.zdev/guidance.md` when present, and harness-native repository instructions. Pass relevant build, run, test, generated-file, and safety guidance to every delegated role.
<!-- zdev:generated-repository-guidance:end -->
